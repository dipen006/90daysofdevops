package main

import "fmt"

func main() {

	// var number = "5"
	// number = number + 1
	// number := do the same things we did just omit the var keyword
	// number := "String hai boss"
	// number := 5 cannot do this if you already stored the value as string
	var number uint64
	var bl bool
	fmt.Println(number, bl)
	// fmt.Printf("%T", number)
}
